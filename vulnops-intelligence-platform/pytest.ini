[pytest]
testpaths = backend/tests data_pipeline/tests ml/tests
pythonpath = backend .
addopts = -m "not integration"
markers =
    integration: requires DATABASE_URL and a reachable PostgreSQL instance
