apiVersion: v1
kind: Service
metadata:
  name: vulnops-web
  namespace: vulnops
  labels:
    app.kubernetes.io/name: vulnops-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vulnops-web
  ports:
    - name: http
      port: 3000
      targetPort: http
