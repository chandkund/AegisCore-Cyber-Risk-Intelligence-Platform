# Create a Kubernetes secret from this file (do not commit real values):
#   kubectl -n aegiscore create secret generic aegiscore-api-secret --from-env-file=secret-api.env
#
# DATABASE_URL=postgresql+psycopg://user:pass@postgres:5432/aegiscore
# JWT_SECRET_KEY=
