apiVersion: v1
kind: Service
metadata:
  name: aegiscore-api
  namespace: aegiscore
  labels:
    app.kubernetes.io/name: aegiscore-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aegiscore-api
  ports:
    - name: http
      port: 8000
      targetPort: http
