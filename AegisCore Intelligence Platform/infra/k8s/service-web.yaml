apiVersion: v1
kind: Service
metadata:
  name: aegiscore-web
  namespace: aegiscore
  labels:
    app.kubernetes.io/name: aegiscore-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aegiscore-web
  ports:
    - name: http
      port: 3000
      targetPort: http
